using System;

/*

Complete the method GetPeopleOrderedByAge(bool descending) in PersonRepository. It should return an IEnumerable<Person> ordered by age ascending
Write each name on a new line in the console. $"{FirstName} {LastName}" 
--

*/

namespace InterviewCoding.Questions
{
    public class Question4
    {
        private readonly PersonRepository _personRepository;
        public Question4()
        {
            _personRepository = new PersonRepository();
        }
        public void Solution()
        {

        }
    }
}